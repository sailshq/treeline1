<!DOCTYPE html>
<html>
  <head>
    <title>Page Not Found</title>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  </head>
  <body>

    <h1>Oh Noes: 404</h1>
    <p>It looks like the route you are trying to hit could not be found. Would you like to create
      the route in Shipyard?</p>

    <p><a href="#">Create on Shipyard</a></p>

    <script type='text/javascript'>
      var yarrData = <%- data %>;


      $('a').click(function() {
        var el = $(this);
        el.text('Creating.....');

        $.ajax({
          type: 'POST',
          url: yarrData.shipyardUrl,
          data: yarrData.data,
          headers: {
            'x-auth': yarrData.xAuth
          },
          success: function(data) {
            window.location = 'http://localhost:1338/projects/' + yarrData.data.project + '/routes/' + data.id;
          },
          dataType: 'json'
        });
      });

    </script>

  </body>
</html>
